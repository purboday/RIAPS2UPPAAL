process Answer_comp_thread(int &ans_port_identity, broadcast chan &msg_type, intq &port_name) {

state
    waiting;
init
    waiting;
trans
    waiting -> waiting { sync msg_type?; assign push(port_name), ans_port_identity = identity; };
}